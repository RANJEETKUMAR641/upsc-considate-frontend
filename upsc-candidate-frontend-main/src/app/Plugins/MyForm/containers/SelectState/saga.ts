// import { take, call, put, select, takeLatest } from 'redux-saga/effects'; //
// import { selectStateActions as actions } from './slice' // function* doSomething()

export function* selectStateSaga() {
  // yield takeLatest(actions.someAction.type,
  // doSomething);
}
