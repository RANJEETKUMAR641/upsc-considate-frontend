import { createGlobalStyle } from 'styled-components'

export const ReactTreeGlobalStyle = createGlobalStyle`

.react-dropdown-tree-select .tree label input[type="checkbox"]{
  pointer-events: none;
  display:none
}

.react-dropdown-tree-select .tree label{
  pointer-events: none;
}

`
